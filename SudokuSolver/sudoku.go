package main

import (
    "fmt"
    "os"
)

var matrix = make([][]int, 9)

func main() {
    file, err := os.Open("sudoku.txt") // For read access.
    if err != nil {
        log.Fatal(err)
    }
    for(int)

}

